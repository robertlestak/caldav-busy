apiVersion: v1
kind: Service
metadata:
  name: {{ include "caldav-busy.fullname" . }}
  labels:
    {{- include "caldav-busy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caldav-busy.selectorLabels" . | nindent 4 }}